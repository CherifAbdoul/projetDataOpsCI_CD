[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-sum-app"
version = "0.1.0"
description = "Simple Python program to sum values"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
sum-app = "main:main"